import styled from 'styled-components'

export const HeaderStyled = styled.header`
  z-index: 10000;
  position: fixed;
  width: 100%;
  top: 0;
  right: 0;
  left: 0;
  padding: 15px;
  margin: 0;
`

export const InnerStyled = styled.header`
  display: flex;
  max-width: 75rem;
  margin: 0 auto;
  align-items: center;
  justify-content: space-between;
`
